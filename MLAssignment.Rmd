---
title: "Machine Learning Course Project"
author: "Coursera Student A"
date: "15 February 2015"
output: html_document
---

#Summary

Using devices such as Jawbone Up, Nike FuelBand, and Fitbit it is now possible to collect a large amount of data about personal activity relatively inexpensively. These type of devices are part of the quantified self movement – a group of enthusiasts who take measurements about themselves regularly to improve their health, to find patterns in their behavior, or because they are tech geeks. One thing that people regularly do is quantify how much of a particular activity they do, but they rarely quantify how well they do it. In this project, your goal will be to use data from accelerometers on the belt, forearm, arm, and dumbell of 6 participants. They were asked to perform barbell lifts correctly and incorrectly in 5 different ways. More information is available from the website here: http://groupware.les.inf.puc-rio.br/har (see the section on the Weight Lifting Exercise Dataset). 



#Data Analysis

```{r library , echo=FALSE, }
require(caret)
require(ggplot2)
require(randomforest)
require(plyr)
require(stringr)
require(knitr)
library(caret)
library(rattle)

```


```{r load data, cache=TRUE ,echo=FALSE}

#The training data for this project are available here: 

trainingURL<-"http://d396qusza40orc.cloudfront.net/predmachlearn/pml-training.csv"
testURL<-"http://d396qusza40orc.cloudfront.net/predmachlearn/pml-testing.csv"
train<-read.csv(trainingURL,na.strings=c("NA",""))
test<-read.csv(testURL,na.strings=c("NA",""))


#Clean data
#clean_train<-apply(!is.na(training),2,sum)==19622


training2<-data.frame(train$user_name,train$classe,train[,grep("accel_",names(train))])

#Remove all var variables as it is a derived column with mostly NA's
training2<-training2[,-(grep("var_",names(training2)))]


#The classe does not exist in the test data set.Do the same filtering for the  test data
testing2<-data.frame(test$user_name,test[,grep("accel_",names(test))])
testing2<-testing2[,-(grep("var_",names(testing2)))]

names(training2)
names(training2)[1] <-"user_name"
names(training2)[2] <-"classe"
training2$classe <-factor(training2$classe)

```

In the analysis the variables were ploted against thier indexes and we can see that the classe is a good classifier for each of the variables. We investigate further to see if  all the variables are required for the prediction.

```{r plots, echo=FALSE}
#knitr::kable(head(training2), booktabs = TRUE ,caption = "Summary Data")
knitr::kable(head(training2[1:6]), booktabs = TRUE ,caption = "Summary Data")
knitr::kable(head(training2[7:12]), booktabs = TRUE )
knitr::kable(head(training2[13:18]), booktabs = TRUE )
###

par(mfrow=c(2,2))
plot(training2$accel_belt_x,col=training2$classe)
plot(training2$accel_belt_y,col=training2$classe)
plot(training2$accel_belt_z,col=training2$classe)

plot(training2$accel_arm_x,col=training2$classe)
plot(training2$accel_arm_y,col=training2$classe)
plot(training2$accel_arm_z,col=training2$classe)

plot(training2$accel_forearm_x,col=training2$classe)
plot(training2$accel_forearm_y,col=training2$classe)
plot(training2$accel_forearm_z,col=training2$classe)

plot(training2$total_accel_forearm,col=training2$classe)
training <-training2

-289 +111-12


inTrain <- createDataPartition(training$classe,p=.75, list=FALSE)


training <- training2[inTrain,]
testing <- training2[-inTrain,]





```


```{r summary, echo=FALSE}

#knitr::kable(summary(training2))

qplot(factor(user_name), data=train,colour=classe)

```

#Model

```{r model, echo=FALSE}



rfFit<- train(classe~., method="rpart",data=training)

print(rfFit)
fancyRpartPlot(rfFit$finalModel)
#knitr::kable(print(rfFit$finalModel))
rfpre <- predict(rfFit,testing)

confusionMatrix(rfpre,testing$classe)
rfFit<-train(classe~.,data=training,method="rf",
                trControl=trainControl(method="cv",number=7),
                prox=TRUE,allowParallel=TRUE)

print(rfFit)
fancyRpartPlot(rfFit$finalModel)
#knitr::kable(print(rfFit$finalModel))
rfpre <- predict(rfFit,testing)

confusionMatrix(rfpre,testing$classe)



```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
